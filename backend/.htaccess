# Root .htaccess for InfinityFree Shared Hosting
# Redirects all incoming traffic to the public/ folder

<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteRule ^$ public/ [L]
  RewriteRule (.*) public/$1 [L]
</IfModule>
